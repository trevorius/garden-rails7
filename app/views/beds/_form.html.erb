<%= form_with(model: bed) do |form| %>
  <% if bed.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bed.errors.count, "error") %> prohibited this bed from being saved:</h2>

      <ul>
        <% bed.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :bed_type, style: "display: block" %>
    <%= form.text_field :bed_type %>
  </div>

  <div>
    <%= form.label :last_watered, style: "display: block" %>
    <%= form.datetime_field :last_watered %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
